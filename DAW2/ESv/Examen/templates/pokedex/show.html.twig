{% extends 'base.html.twig' %}

{% block title %}Pokedex
{% endblock %}

{% block body %}
	<h1>Pokedex</h1>

	<a href="{{path('app_equipo_show', {'id': pokedex.owner.id})}}">Ver tu equipo</a>

	{% for pokemon in pokedex.pokemons %}
		<div style="border: 1px solid black;">
			<h3>Nombre:
				{{pokemon.name}}</h3>
			<img src="/img/{{pokemon.imagen}}"/>

			{% if app.user.equipo|length < 6 %}

				<a href="{{path('app_equipo_edit', {'id':pokemon.id})}}">Añadir al equipo</a>

			{% endif %}

		</div>
	{% endfor %}

	<a href="{{ path('app_pokemon_index') }}">Volver a pokemons</a>

	{# <a href="{{ path('app_pokedex_edit', {'id': pokedex.id}) }}">edit</a> #}

	{# {{ include('pokedex/_delete_form.html.twig') }} #}
{% endblock %}
