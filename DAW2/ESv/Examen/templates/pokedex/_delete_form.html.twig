<form method="post" action="{{ path('app_pokedex_delete', {'id': pokedex.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pokedex.id) }}">
    <button class="btn">Delete</button>
</form>
