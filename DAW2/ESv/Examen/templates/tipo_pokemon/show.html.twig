{% extends 'base.html.twig' %}

{% block title %}TipoPokemon{% endblock %}

{% block body %}
    <h1>TipoPokemon</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tipo_pokemon.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ tipo_pokemon.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_tipo_pokemon_index') }}">back to list</a>

    <a href="{{ path('app_tipo_pokemon_edit', {'id': tipo_pokemon.id}) }}">edit</a>

    {{ include('tipo_pokemon/_delete_form.html.twig') }}
{% endblock %}
