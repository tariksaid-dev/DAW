{% extends 'base.html.twig' %}

{% block title %}Line{% endblock %}

{% block body %}
    <h1>Line</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ line.id }}</td>
            </tr>
            <tr>
                <th>Cantidad</th>
                <td>{{ line.cantidad }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_line_index') }}">back to list</a>

    <a href="{{ path('app_line_edit', {'id': line.id}) }}">edit</a>

    {{ include('line/_delete_form.html.twig') }}
{% endblock %}
